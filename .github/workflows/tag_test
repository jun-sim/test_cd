name: Release Tag
on:
  push:
    tags:
      - dev-rc**
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: test
      run: echo ${{github.event.inputs.tags}}
      id: tag_name 
